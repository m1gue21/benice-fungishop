---
---

<section id="inicio" class="relative min-h-screen flex items-center justify-center overflow-hidden">
  <!-- Background Pattern -->
  <div class="absolute inset-0 bg-gradient-to-br from-sage-50 to-cream-100">
    <div class="absolute inset-0 opacity-10">
      <svg class="w-full h-full" viewBox="0 0 100 100" fill="none">
        <circle cx="20" cy="20" r="2" fill="currentColor" class="text-sage-400 animate-float"/>
        <circle cx="80" cy="30" r="1.5" fill="currentColor" class="text-earth-400"/>
        <circle cx="60" cy="70" r="2.5" fill="currentColor" class="text-sage-300 animate-float"/>
        <circle cx="30" cy="80" r="1" fill="currentColor" class="text-earth-300"/>
        <circle cx="90" cy="80" r="2" fill="currentColor" class="text-sage-400"/>
      </svg>
    </div>
  </div>

  <div class="container mx-auto px-4 py-20 relative z-10">
    <div class="max-w-4xl mx-auto text-center">
      <!-- Main Logo/Brand -->
      <div class="mb-8 animate-fade-in">
        <div class="w-32 h-32 mx-auto mb-6 rounded-full bg-gradient-to-br from-sage-400 via-earth-400 to-sage-500 p-1">
          <div class="w-full h-full rounded-full bg-white flex items-center justify-center">
            <svg class="w-16 h-16 text-earth-600" fill="currentColor" viewBox="0 0 24 24">
              <path d="M12 2L13.09 8.26L17.65 7.35L15.17 11.17L19.5 12L15.17 12.83L17.65 16.65L13.09 15.74L12 22L10.91 15.74L6.35 16.65L8.83 12.83L4.5 12L8.83 11.17L6.35 7.35L10.91 8.26L12 2Z"/>
            </svg>
          </div>
        </div>
        <h1 class="font-serif text-5xl md:text-7xl font-bold text-earth-800 mb-4">
          Be Nice
        </h1>
        <p class="font-serif text-2xl md:text-3xl text-sage-600 mb-2">Fungi Shop</p>
        <div class="w-24 h-0.5 bg-gradient-to-r from-sage-400 to-earth-400 mx-auto"></div>
      </div>

      <!-- Main Message -->
      <div class="mb-12 animate-slide-up">
        <h2 class="text-3xl md:text-4xl font-serif font-semibold text-earth-800 mb-6">
          Eleva tu bienestar naturalmente
        </h2>
        <p class="text-lg md:text-xl text-earth-700 max-w-2xl mx-auto leading-relaxed">
          Descubre el poder ancestral de los hongos funcionales y adaptógenos. 
          Productos de la más alta calidad para potenciar tu mente, cuerpo y espíritu.
        </p>
      </div>

      <!-- CTA Buttons -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center mb-16 animate-slide-up">
        <button 
          class="bg-sage-500 hover:bg-sage-600 text-white px-8 py-4 rounded-full font-semibold text-lg transition-all duration-300 hover:scale-105 hover:shadow-lg"
          onclick="scrollToSection('productos')"
        >
          Explorar Productos
        </button>
        <button 
          class="border-2 border-sage-500 text-sage-600 hover:bg-sage-500 hover:text-white px-8 py-4 rounded-full font-semibold text-lg transition-all duration-300 hover:scale-105"
          onclick="scrollToSection('educacion')"
        >
          Aprender Más
        </button>
      </div>

      <!-- Features -->
      <div class="grid md:grid-cols-3 gap-8 max-w-3xl mx-auto animate-fade-in">
        <div class="text-center group">
          <div class="w-16 h-16 bg-gradient-to-br from-sage-400 to-earth-400 rounded-full flex items-center justify-center mx-auto mb-4 group-hover:scale-110 transition-transform duration-300">
            <svg class="w-8 h-8 text-white" fill="currentColor" viewBox="0 0 24 24">
              <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
            </svg>
          </div>
          <h3 class="font-serif text-xl font-semibold text-earth-800 mb-2">100% Natural</h3>
          <p class="text-earth-600">Productos puros sin aditivos artificiales</p>
        </div>
        
        <div class="text-center group">
          <div class="w-16 h-16 bg-gradient-to-br from-sage-400 to-earth-400 rounded-full flex items-center justify-center mx-auto mb-4 group-hover:scale-110 transition-transform duration-300">
            <svg class="w-8 h-8 text-white" fill="currentColor" viewBox="0 0 24 24">
              <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
          </div>
          <h3 class="font-serif text-xl font-semibold text-earth-800 mb-2">Calidad Premium</h3>
          <p class="text-earth-600">Rigurosos controles de calidad</p>
        </div>
        
        <div class="text-center group">
          <div class="w-16 h-16 bg-gradient-to-br from-sage-400 to-earth-400 rounded-full flex items-center justify-center mx-auto mb-4 group-hover:scale-110 transition-transform duration-300">
            <svg class="w-8 h-8 text-white" fill="currentColor" viewBox="0 0 24 24">
              <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
            </svg>
          </div>
          <h3 class="font-serif text-xl font-semibold text-earth-800 mb-2">Respaldo Científico</h3>
          <p class="text-earth-600">Basado en investigación moderna</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
    <svg class="w-6 h-6 text-sage-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"/>
    </svg>
  </div>
</section>

<script>
  function scrollToSection(id) {
    const element = document.getElementById(id);
    if (element) {
      element.scrollIntoView({ behavior: 'smooth' });
    }
  }
</script>